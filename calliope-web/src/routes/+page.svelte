<script lang="ts">
	// The root route redirects to /agents in +page.ts. This empty component only
	// exists to satisfy the leaf-route requirement; it is never rendered because
	// load() throws redirect first. Kept as a defensive fallback.
</script>
